# 02 · Todo 归档

## [已完成] popup 日志复制重构：双按钮 + lastUserSample + 全量去重

日期：2026-09-15 · 完成：v0.2.4（commit d4aedef）

### 落地内容（与设计对齐）

- 双按钮：「复制 (尾5条)」（原「复制取证」，逻辑不变）+「复制(全部)」（原「复制」，取 popup 全部日志 + 去重）
- `LogEntry.lastUserSample`：最后一条 user 消息前 200 字（`router.ts` `lastUserSampleOf`，array content 经 renderMessageContent 渲染，不泄 data URL）；两处 log 写入点都带；进 `FORENSIC_FIELDS`
- `slimFullCopy`（snippet.ts，复制层去重，不动 LogEntry 写入）：mirrorFull 全排除；messagesFull 仅每条 cid 最后一条保留（无 cid 条目各算一组）
- popup 日志列表每条渲染 `req=`（requestFull：reasoning/search/modelType/toolChoice/tools/refFileIds/promptLen）与 `user=`（lastUserSample）行，均转义
- 验证：41 文件 / 429 用例全绿；tsc 0 错；bundle 三项核验（sw.js 时间戳 / lastUserSample / 按钮标签 \uXXXX 形态）
