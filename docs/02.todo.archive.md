# 02 · Todo 归档

## [已完成] DSML 闭标签漂移形态未被检测 → 原文透传（第 5 形态）

日期：2026-09-15 · 完成：v0.2.5（commit a7ef32d + 9041049，origin/main）

### 落地内容（与 spec §4.4 对齐）

- 现场：spice 报告模型开**标准** `<tool_calls>`（prompt 教的形态）+ 块内合法 OpenAI JSON，
  收尾幻觉出漂移形态 DSML **闭标签** `</｜｜DSML｜｜ parameter>` / `</｜｜DSML｜｜ invoke>` /
  `</｜｜DSML｜｜ calls>`（`<` 与竖线间有 `/`，v0.1.100 开标签漂移同形但在闭合位置）。
  旧检测正则只认开标签 → `hasToolTags=false` → 判「没调工具」→ 原文透传 + `stop`，
  spice 整轮停死（违反「绝不静默透传」不变量的检测盲区，非设计缺口）。
- 修法：**仅检测层** `dsmlMarkerRe` / `invokeMarkRe` 的 `<` 放宽为 `</?`；解析保持严格，
  命中后落既有 repair/400 通路（流式路径假块扣进 `unparsed` 供 repair 输入）。
- spec `2026-09-08-deep-api-extension-design.md` §4.4 补第 5 形态记录。
- 验证：新增 8 个回归测试（dsml-parser 6 + tool-pipeline 2，base64 字节级 fixture +
  孤立闭标签 + 散文 `</tool_calls>` 不误判护栏）；全量 41 文件 / 437 用例绿；tsc 0 错；
  bundle 双正则 `</?` 已在 extension/sw.js 验证。归属决策：用户拍板由 deep.api 修
  （延续 v0.1.102 归属：适配层抽象不漏给使用方）；spice 侧「收到畸形就停死」另行反馈，属纵深防御。

## [已完成] popup 日志复制重构：双按钮 + lastUserSample + 全量去重

日期：2026-09-15 · 完成：v0.2.4（commit d4aedef）

### 落地内容（与设计对齐）

- 双按钮：「复制 (尾5条)」（原「复制取证」，逻辑不变）+「复制(全部)」（原「复制」，取 popup 全部日志 + 去重）
- `LogEntry.lastUserSample`：最后一条 user 消息前 200 字（`router.ts` `lastUserSampleOf`，array content 经 renderMessageContent 渲染，不泄 data URL）；两处 log 写入点都带；进 `FORENSIC_FIELDS`
- `slimFullCopy`（snippet.ts，复制层去重，不动 LogEntry 写入）：mirrorFull 全排除；messagesFull 仅每条 cid 最后一条保留（无 cid 条目各算一组）
- popup 日志列表每条渲染 `req=`（requestFull：reasoning/search/modelType/toolChoice/tools/refFileIds/promptLen）与 `user=`（lastUserSample）行，均转义
- 验证：41 文件 / 429 用例全绿；tsc 0 错；bundle 三项核验（sw.js 时间戳 / lastUserSample / 按钮标签 \uXXXX 形态）
