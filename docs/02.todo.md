# 待定任务

- [x] **tool-pipeline instruction 硬化：禁止无工具调用的行动宣言 + 丢弃配对空调用块**（2026-09-12，**交付：commit 909a47b，origin/main**）：`buildToolPrompt` 的 `auto` 分支 instruction 追加「宣言必须同消息携带 `<tool_calls>`；无工具块纯文本只允许最终总结或向用户提问」；归一化器对配对空块 `<tool_calls></tool_calls>` 从散文豁免透传改为丢弃记录 `dropped`（trace #260 现场污染修复）。spec：`docs/superpowers/specs/2026-09-12-no-announcement-without-tools-design.md`（主修在 spice 仓：harness 自动 nudge，见其 spec `2026-09-12-agent-stall-autonudge-design.md`）。
