# 待定任务

## [待定] thinking 截断自动续接（DeepSeek 网页 web API）

日期：2026-09-11 · 状态：spike 完成，待抓包验证 + 写 spec

### 问题

DeepSeek 网页 thinking 模式输出超长（~10 万字）时，服务端断流 + UI 显示 Continue 按钮，需用户手动点。deep.api 当前识别不了这个信号（`sse-patch.ts:96` 写了 `response/status` path 但没消费 value），客户端拿到的是 `finish_reason: 'stop'` 的截断结果，silent bug。

### Spike 产出

- `.research/continue-mechanism.md` — 外部调研（llmweb2api / deepseek-pp / DeepSeek 官方 docs）
- `.research/spike-design.md` — 完整 spike 报告（结论：deep.api 是第一个需要处理这个问题的项目；~95 行生产 + ~120 行测试的双轨方案；含抓包模板）

### 下一步

1. **人工抓包**：DevTools Network 触发一次 thinking 截断 + 点 Continue，按 `spike-design.md §4.3` yaml 模板填四段
2. **冻结设计**：从 spike 转 bounded，写 `docs/superpowers/specs/2026-09-11-continue-thinking-design.md`
3. **走 writing-plans**：出实施计划 + TDD

### 触发条件

- 用户方便登录 DeepSeek 网页时一次性完成（约 30 分钟）
- 不紧急；当前线（v0.1.104）不做也不影响日常使用

### 参考

- spec：`.research/spike-design.md`
- 相关代码：`src/background/providers/deepseek/sse-patch.ts:96`（未消费的 status 路径）、`src/background/router.ts:499`（repairToolCalls 是同款"reuse session + new parent"模式可复用）